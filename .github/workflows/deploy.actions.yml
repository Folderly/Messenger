name: Folderly Messenger Client

on:
  workflow_dispatch:
    inputs:
      build:
        description: "Build"
        required: true
        type: boolean
        default: true
      rollout:
        description: "Rollout"
        required: false
        type: boolean
        default: false

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Log level: $BUILD"
          echo "Tags: $ROLLOUT"
        env:
          BUILD: ${{ github.event.inputs.build }}
          ROLLOUT: ${{ github.event.inputs.rollout }}

  build:
    if: ${{ github.event.inputs.build == true }}
    runs-on: ubuntu-latest
    env:
      GCP_IMAGE_DESTINATION: ${{ secrets.GCP_IMAGE_DESTINATION }}

    permissions:
      contents: "read"
      id-token: "write"

    steps:
      - uses: actions/checkout@v3
      - id: "auth"
        name: "Authenticate to Google Cloud"
        uses: "google-github-actions/auth@v0"
        with:
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.SERVICE_ACCOUNT }}

  # rollout:
  #   needs: build
  #   if: github.event_name == 'rollout'
  #   runs-on: ubuntu-latest
